{
  "schemaVersion": 1,
  "id": "popt",
  "version": "${version}",
  "name": "POPT",
  "description": "Prediction projectile trajectory",
  "authors": [],
  "contact": {},
  "license": "GPL-3.0",
  "icon": "assets/popt/icon.png",
  "environment": "client",
  "entrypoints": {
    "client": [
      "top.o_illusions.popt.client.PoptClient"
    ],
    "main": [
      "top.o_illusions.popt.Popt"
    ]
  },
  "mixins": [
    "popt.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric": "*",
    "minecraft": "${minecraft_version}"
  }
}
